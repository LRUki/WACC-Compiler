# possible path for no return from function

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  int f() is
    if (1 == 1) then
      println 'a'
     else
      if (5 == 0) then
        return 2
        else
        return 3
        fi
     fi
  end
  
  int x = call f()
end
