# simple struct declaration and assignment

# Output:
# #empty#

# Exit:
# 20

# Program:

begin
  struct Person {int age; char grade;};
  struct Person Bob = {42, 'A'};
  Bob.age = 20;
  exit Bob.age
end