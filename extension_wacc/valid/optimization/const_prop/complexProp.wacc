# testing a complex program which can be optimised using constant propagation

# Output:
# #empty#

# Exit:
# 558

begin
  int x = 69;
  int y = x + 420;
  int z = x + y + x;
  bool ty = true;
  int uu = 78;
  if ty then
    uu = 56436;
    int p = x + y + z + 82
      else
    int er = uu + y
  fi;

  bool nt = false;
  while ty && !nt do
    bool newBool = !ty;
    string r = "fsdg";
    int[] tr = [1,54,4,7,9,9,8]
   done;
  exit z
end

