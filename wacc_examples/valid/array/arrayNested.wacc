# basic array declaration and assignment

# Output:
# 3
# 3

# Program:

begin
  # int[] a = [1,2,3];
  pair(pair, int) a = newpair(null, 2);
  pair(pair, char) d  = newpair(a, 'b') ;
  int[] b = [3,4];
  int[][] c = [a,b] ;
  println c[0][2] ;
  println c[1][0]
end

